{
    IBClasses = (
        {
            ACTIONS = {
                addWord = id; 
                ignore = id; 
                ignoreAll = id; 
                showPreferences = id; 
                toggleShowAllURLS = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}